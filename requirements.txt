# Union of the three apps' runtime deps + the Streamlit UI. One environment runs
# the whole chain in-process (see pipeline_core.py).
streamlit>=1.50
plotly>=5.18.0
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.6.0
xgboost>=2.0.0
joblib>=1.3.0
python-dotenv>=1.0.0
pyyaml>=6.0
anthropic>=0.40.0
python-docx>=1.1.0
rich>=13.7.0
