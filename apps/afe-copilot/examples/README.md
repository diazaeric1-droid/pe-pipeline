# Example Diagnoses

Drop-in input files for the AFE Drafter.

In production these would be produced by the Production Engineer Copilot's well-review output, mapped one-to-one into the AFE Drafter's input schema. Run with:

```bash
python -m src.drafter --input examples/well_diagnosis_001.json --out drafts/ --docx --verbose
```
